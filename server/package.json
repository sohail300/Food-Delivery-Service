{
  "name": "instafood-server",
  "version": "1.0.0",
  "description": "A node.js web server that fetches data from a third-party (Swiggy) API and exposes it to a client app via an HTTP endpoint.",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "npm i",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^3.1.5",
    "express": "^4.18.2"
  }
}
